{
  "name": "prefw/example-headless-task",
  "description": "Example Headless Task Bundle for System prefw.",
  "version": "1.0.0",
  "type": "prefw-task",
  "require": {
    "prefw/task-installer-plugin": "*"
  },
  "extra": {
    "name": "Example Headless Task",
    "type": "Headless",
    "endpoint": {
      "controller": "HeadlessController"
    },
    "options": {
      "schema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "title": "Log Path",
            "description": "Traverse into object path from the data store in the current work instance, and log that into database."
          }
        }
      },
      "form": ["*"]
    }
  }
}
